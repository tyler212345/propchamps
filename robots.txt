# PropChamps — robots.txt
# Allow all reputable crawlers, including AI assistants (GPTBot, ClaudeBot,
# PerplexityBot, etc.) so PropChamps can be surfaced and cited in AI answers.
# Point everyone to the sitemap.

User-agent: *
Allow: /

# NOTE: Do NOT disallow /data/ — every firm page loads /data/firms.js at render
# time to populate its content. Blocking it makes Googlebot render an empty
# "Firm not found" page and flags every firm page as a Soft 404. Leave it open.

Sitemap: https://propchamps.net/sitemap.xml
